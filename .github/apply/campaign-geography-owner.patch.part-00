diff --git a/contracts/json_schema/manifest.json b/contracts/json_schema/manifest.json
index 68f1758..8e42949 100644
--- a/contracts/json_schema/manifest.json
+++ b/contracts/json_schema/manifest.json
@@ -38,6 +38,11 @@
       "model": "collection_contracts.observations.ExtractionRequest",
       "sha256": "sha256:d064cb7b0eba353db3fecf6f704520d8d6fec5e313e09a4713597c6359717f0a"
     },
+    {
+      "file": "geography-document.schema.json",
+      "model": "collection_contracts.campaign_config.GeographyDocument",
+      "sha256": "sha256:6eb66324fd79bbe88066da94fe2c737f72f5d7c8095eba7a4dc6df2821cf5338"
+    },
     {
       "file": "manual-import-plan.schema.json",
       "model": "collection_contracts.manual_import.ManualImportPlan",
diff --git a/packages/collection_application/src/collection_application/campaign_documents.py b/packages/collection_application/src/collection_application/campaign_documents.py
index f99a043..6d4b06c 100644
--- a/packages/collection_application/src/collection_application/campaign_documents.py
+++ b/packages/collection_application/src/collection_application/campaign_documents.py
@@ -1,17 +1,22 @@
 from __future__ import annotations
 
+import json
 from collections.abc import Callable
 from dataclasses import dataclass
+from hashlib import sha256
 from typing import cast
 
 import yaml
 from pydantic import BaseModel, ValidationError
 
+from collection_application.geography import GeographyBoundaryError, decode_boundary_geojson
 from collection_application.ports import RawCampaignBundle
 from collection_contracts import (
     AttributesDocument,
     CampaignDocument,
     EntityKindsDocument,
+    GeographyDocument,
+    GeographyProvenanceDocument,
     SourceBindingsDocument,
     SourcePolicy,
     TaxonomyDocument,
@@ -23,6 +28,7 @@ ENTITY_KINDS_DOCUMENT = "entity_kinds.yaml"
 TAXONOMY_DOCUMENT = "taxonomy.yaml"
 ATTRIBUTES_DOCUMENT = "attributes.yaml"
 SOURCE_BINDINGS_DOCUMENT = "source_bindings.yaml"
+GEOGRAPHY_DOCUMENT = "geography.yaml"
 REQUIRED_DOCUMENTS = frozenset(
     {
         CAMPAIGN_DOCUMENT,
@@ -42,6 +48,8 @@ class ParsedCampaignDocuments:
     attributes: AttributesDocument
     source_bindings: SourceBindingsDocument
     source_policies: dict[str, SourcePolicy]
+    geography: GeographyDocument | None
+    geography_artifacts: dict[str, object]
 
 
 class _UniqueKeyLoader(yaml.SafeLoader):
@@ -88,6 +96,7 @@ def parse_campaign_documents(
     correlation_id: str,
 ) -> ParsedCampaignDocuments:
     _require_documents(bundle, correlation_id)
+    geography, geography_artifacts = _load_geography(bundle, correlation_id)
     return ParsedCampaignDocuments(
         campaign=_load_model(bundle, CAMPAIGN_DOCUMENT, CampaignDocument, correlation_id),
         entity_kinds=_load_model(
@@ -105,6 +114,8 @@ def parse_campaign_documents(
             correlation_id,
         ),
         source_policies=_load_source_policies(bundle, correlation_id),
+        geography=geography,
+        geography_artifacts=geography_artifacts,
     )
 
 
@@ -130,6 +141,16 @@ def _load_model[ModelT: BaseModel](
     correlation_id: str,
 ) -> ModelT:
     payload = _load_yaml(bundle.files[path], path, correlation_id)
+    return _validate_model(bundle, path, payload, model_type, correlation_id)
+
+
+def _validate_model[ModelT: BaseModel](
+    bundle: RawCampaignBundle,
+    path: str,
+    payload: object,
+    model_type: type[ModelT],
+    correlation_id: str,
+) -> ModelT:
     try:
         return model_typ