name: Export Admission Source

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/export-admission-source.yml

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Upload exact source snapshot
        uses: actions/upload-artifact@v4
        with:
          name: data-collection-platform-admission-source
          path: .
          include-hidden-files: true
          retention-days: 1
