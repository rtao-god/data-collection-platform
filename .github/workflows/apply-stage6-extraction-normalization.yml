name: Apply Stage 6 Extraction and Normalization

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/apply-stage6-extraction-normalization.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: data-collection-platform-stage6-extraction-apply
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out latest main
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 20

      - name: Install exact Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405
        with:
          python-version-file: .python-version
          check-latest: false

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
        with:
          version: "0.10.0"
          enable-cache: false

      - name: Materialize extraction and normalization owners
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          git reset --hard origin/main

          python .github/stage6/apply_extraction_normalization.py
          python .github/stage6/patch_extraction_normalization.py
          python .github/stage6/patch_extraction_normalization_part2.py

          python - <<'PY'
          import subprocess
          import sys
          from pathlib import Path

          checker = Path("tools/architecture_checks/check_dependencies.py")
          policy = subprocess.check_output(
              [sys.executable, str(checker), "--print-policy"],
              text=True,
          ).strip()
          path = Path("docs/architecture/dependency-rules.md")
          text = path.read_text(encoding="utf-8")
          start_marker = "<!-- dependency-policy:start -->"
          end_marker = "<!-- dependency-policy:end -->"
          start = text.index(start_marker)
          end = text.index(end_marker, start) + len(end_marker)
          path.write_text(text[:start] + policy + text[end:], encoding="utf-8")
          PY

          uv lock
          uv sync --frozen --all-packages --dev
          uv run ruff check --exclude .github/stage6 . --fix --unsafe-fixes
          uv run ruff format --exclude .github/stage6 .
          uv run ruff check --exclude .github/stage6 .
          uv run mypy
          uv run pytest -m "not integration and not object_store_integration"
          uv run python tools/architecture_checks/check_dependencies.py
          uv run python tools/contract_generation/generate.py --check
          uv run python -m compileall -q apps connectors database packages tools
          uv run collector config validate berlin_recording_services
          docker build --file deploy/docker/extraction-worker.Dockerfile .
          git diff --check

          git add -A
          git diff --cached --check
          git config user.name "rtao-god"
          git config user.email "rtaogod@gmail.com"
          git commit -m "Feat (Extraction): add evidence-bound observation worker"
          git push origin HEAD:main
